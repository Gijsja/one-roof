# Architecture Decision Log

Append decisions; do not silently rewrite history. Use `Handoffs/DECISION_TEMPLATE.md` for full records when needed.

| ID | Status | Decision | Reason |
| --- | --- | --- | --- |
| ADR-001 | Accepted | Pure C# domain separate from Unity presentation | Scale, testability, save stability |
| ADR-002 | Accepted | Stable integer runtime IDs and immutable string content IDs | Decouple simulation from assets and scenes |
| ADR-003 | Accepted | Hierarchical floor/transit routing | Make vertical travel scalable and inspectable |
| ADR-004 | Accepted | Persistent simulation entities with pooled visible views | Support many lives without many GameObjects |
| ADR-005 | Accepted | ScriptableObjects for definitions only | Prevent mutable campaign state leaking into assets |
| ADR-006 | Accepted | First playable precedes factions and asset generation | Prove the core cause-and-effect loop first |
| ADR-007 | Proposed | Unity 6 LTS + URP + 2D Animation + Addressables | Matches 2.5D presentation and content volume; confirm exact versions after project creation |
| ADR-008 | Accepted | Ship the initial release as a premium single-player desktop game using Unity 6000.3.24f1, URP, and GitHub-hosted Git | Confirms OR-001 from the product vision, installed project metadata, and established repository workflow |
| ADR-009 | Accepted | Use Unity Addressables 4.0.1 and 2D Animation 13.0.6 | These approved Package Manager resolutions implement ADR-007 for the current Unity 6000.3.24f1 project |
| ADR-010 | Accepted | Separate runtime concerns into named assemblies with pure Domain and Application layers | Compile-time references enforce the architecture boundary before simulation systems are added |
| ADR-011 | Accepted | Run Unity validation in GitHub Actions with GameCI v4 | The workflow tests Edit Mode and Play Mode separately, then compiles a Linux player from a clean checkout |
| ADR-012 | Accepted | Validate primitive values at construction; preserve random streams as xorshift64 seed/state/position snapshots | Gives save-local contracts unambiguous invalid-value boundaries and exact, portable replay after save/load |
| ADR-013 | Accepted | Expose transit simulation to presentation through Application projections | Keeps presentation dependent on read-only data and capacity actions without leaking mutable Domain state or Domain assembly types across the boundary |
| ADR-014 | Accepted | Model building spaces with discrete 1D cell bounds per floor and typed portal thresholds | Supports non-overlapping rooms, vertical and horizontal portal graphs, and deterministic pathfinding without continuous physics |
| ADR-015 | Accepted | Use a typed JSON envelope with embedded payload string, forward migrator pipeline, and atomic file replace | Guarantees clean corruption rejection, schema version evolution without full model reflection, and safe writes via temporary files |
| ADR-016 | Accepted | Connect horizontal walk edges and vertical elevator shaft edges into a unified hierarchical transit graph | Allows deterministic multi-floor route planning with explicit walk and elevator transit legs without continuous physics |
| ADR-017 | Accepted | Implement elevator banks as explicit multi-phase state machines with discrete travel, door cycle, and dwell ticks | Enforces strict car capacity, FIFO floor queuing, and deterministic transit timing |
| ADR-018 | Accepted | Project transit congestion through read-only floor and bank projections with discrete severity tiers | Enables explainable cause diagnosis (queue length and wait time) for presentation overlays and inspectors |
| ADR-019 | Accepted | Model PersonRecord and HouseholdRecord as mutable sealed classes with explicit mutation methods, not structs or property setters | Keeps simulation ticks allocation-free during steady-state while retaining simple object-graph semantics before NativeCollections are required |
| ADR-020 | Accepted | ScheduleTripGenerator detects block transitions by comparing adjacent tick labels rather than caching per-person previous-block state | Keeps the service stateless, safe to call at any tick, and trivially testable without session initialisation |
| ADR-021 | Accepted | NpcViewPool with 40-view cap bound to NpcProjection stream via NpcVisibilityPolicy | Enforces the 40-view performance budget (Docs/02_ARCHITECTURE.md) while 50 persistent simulation entities run in Domain, recycling pooled Unity GameObjects based on floor visibility and activity priority |
| ADR-022 | Accepted | Command-driven ModeShellSession managing Build, Inspect, and Data modes | Dispatches UI commands and emits read-only ModeShellProjection snapshots, ensuring UI cannot mutate simulation state directly |
| ADR-023 | Accepted | ElevatorWaitOverlayService and CongestionInspectorCardView exposing symptom-cause-response chain | Delivers non-color accessible queue and flow indicators alongside root-cause diagnosis and direct routes to Build mode |
| ADR-024 | Accepted | Deterministic ElevatorPlacementPredictor with before/after wait metrics | Calculates throughput deltas, wait time reductions, and confidence labeling for placement previews before player confirms capacity additions |
| ADR-025 | Accepted | Command-driven dynamic topology mutation and graph synchronization via BuildingTopologyState | Validates floor continuity, slab bounds, room overlap, and structural support before mutating state, emitting domain events and lazily synchronizing HierarchicalTransitGraph |
| ADR-026 | Accepted | Master TowerSimulation and TransitExecutionSystem for discrete leg-by-leg transit movement | Coordinates simulation clock, routine schedule transitions, hierarchical route planning, and discrete leg-by-leg execution (walk progress and elevator queuing/boarding) without allocations |
| ADR-027 | Accepted | Tower economy treasury and autonomous demand-driven leasing system | Deducts construction costs from a cash balance, collects periodic rent, and autonomously evaluates apartment vacancies against transit congestion to move in households |
| ADR-028 | Accepted | Comprehensive in-flight commute and dynamic architecture persistence via TowerSaveData | Serializes building topology, population records, economy treasury, active in-flight trip legs, and elevator car/queue passengers for exact deterministic replay |
| ADR-029 | Accepted | GridPlacementController and PlacementGhostPresenter for interactive build mode | Raycasts screen space to discrete CellCoordinates, validates against slabs, rooms, and treasury, renders colored visual ghosts, and dispatches validated domain commands |
| ADR-030 | Accepted | Golden expansion & persistence acceptance verification for M5.1 | Validates the complete vertical city loop: dynamic 6th-floor construction, demand-driven leasing expansion, commute congestion, >40% wait reduction on elevator intervention, and bit-exact save/load replay |
| ADR-031 | Accepted | Spine 2D skeletal setup, 8-layer NPC contract, and generated resident sprite assets | Replaces placeholder rectangular bars with normalized character sprites structured around a shared 17-bone Spine 4.2 rig (rig.npc.humanoid.2d.v1) and the 8-layer slot contract (Docs/05_ASSET_PIPELINE.md), using NpcContentRegistry, ResidentSpriteCatalog, and NpcSkeletalHierarchy with transit status plates |
| ADR-032 | Accepted | 9-Sliced room backdrop presenter, architectural fixtures, and interactive camera navigation | Replaces primitive room quads with 9-sliced SpriteRenderers (Rooms/), door/window fixtures (Architecture/), and implements TowerCameraController for WASD/mouse pan and scroll zoom |
| ADR-033 | Accepted | Domain interaction point anchors and environment prop furnishings | Declares InteractionPoint domain schema for resident docking and implements PropContentRegistry, PropCatalog, and RoomFurnishingPresenter placing 16 environment props across themed rooms |
| ADR-034 | Accepted | Spatial resident room occupancy and leg-by-leg corridor elevator transit | Replaces arbitrary corridor stacking by placing non-commuting residents inside their designated apartments/offices/diner with interior slot distribution, dynamically projecting walking, floor-specific elevator queues, and in-cabin riding |
| ADR-035 | Accepted | Resident emote balloons and reaction icons for ambient emotional legibility and congestion symptoms | Adds 24 pixel-art emote balloons with 3-frame animations across 8 categories, mapped to NpcEmoteKind in OneRoof.Content, EmoteContentRegistry, EmoteSpriteCatalog in OneRoof.Presentation, and rendered via NpcSkeletalHierarchy EmoteBubble anchor driven by transit wait ticks (... -> sweat -> anger) and room activities |
| ADR-036 | Accepted | Delete TransitPrototypeSimulation; TowerSimulationSession is the single session module | The prototype simulation was an unjustified seam maintaining redundant projection mappings that risked silent divergence from the real TowerSimulation. Renamed TransitPrototypeProjection to TowerProjection. Testbed_Transit scene requires Editor rebuild |
| ADR-037 | Accepted | Split TowerPlayableController into 4 focused deep presenters and a dedicated HUD view | God presenter mixed structural slabs, elevator shafts, room rendering, resident views, camera, and OnGUI HUD in an 1100+ line monolith. Extracted TowerStructurePresenter, ElevatorBankPresenter, RoomPresenter, TowerResidentPresenter, and TowerDashboardHudView; reduced TowerPlayableController to 150 lines |
| ADR-038 | Accepted | Lift placement validation behind Domain CanExecute seam | Presentation GridPlacementController duplicated ~200 lines of placement rules (slab bounds, continuous structural support, room overlap, funds, elevator bank car limits). Unified validation under ICommand and TowerSimulation.CanExecute(ICommand), with typed CanExecute methods on BuildingTopologyState and consistent domain rejection messages |
| ADR-039 | Accepted | Encapsulate serialization into aggregates via ToSaveData and FromSaveData | TowerSimulation root previously manually translated ~150 lines of aggregate internal fields into save arrays. Created TopologySaveData, PopulationSaveData, EconomySaveData, TransitSaveData and moved serialization ownership directly into BuildingTopologyState, PopulationState, TowerEconomyState, ElevatorBank, and TransitExecutionSystem; reduced TowerSimulation export/restore to thin orchestration |
| ADR-040 | Accepted | Encapsulate ElevatorBank queue internals behind Snapshot() | ElevatorBank exposed raw mutable Queue<ElevatorPassenger> via FloorQueues and List<ElevatorPassenger> via DeliveredPassengers. Made internal to Domain assembly, introduced immutable ElevatorBankSnapshot with queue and passenger lookup helpers, and updated TowerSimulationSession projection builder |
| ADR-041 | Accepted | Dynamic elevator car horizontal layout clamped inside shaft bounds | Elevator cars dynamically scale width and spacing based on car count within discrete shaft column bounds [-2.32 .. -1.48], preventing multi-car visual clipping into adjacent rooms |
| ADR-042 | Accepted | Bind visual elevator shaft span to ElevatorBank min and max serviced floors | Shaft presentation quads, penthouse cap, and pit cap adapt to the active elevator floor range rather than assuming all building floors, eliminating visual phantom shafts on upper expansions |
| ADR-043 | Accepted | Strict shaft-column room rejection and adjacent-floor stairwalk continuity | BuildingTopologyState blocks any room placement overlapping elevator shaft columns; HierarchicalTransitGraph restricts vertical stairwell walk edges to adjacent floors (|floor_a - floor_b| == 1) |
| ADR-044 | Accepted | Keep visual transitions, congestion aura, and furniture docking presentation-only | View effects derive only from immutable transit/topology projections; interaction-point docking consumes domain anchors without storing Unity references in simulation state. |
| ADR-045 | Accepted | Build deep inspection cards from immutable application projections | Resident, room, and elevator-bank cards receive only formatted projection data; elevator details are sourced through `ElevatorBankSnapshot`, keeping UI outside mutable domain state. |
| ADR-046 | Accepted | Use a shared procedural 17-bone rig with immutable eight-layer wardrobe loadouts | Every resident resolves compatible layer IDs from approved content records; presentation owns swatches, slot ordering, and the six common animation states without putting visual state in simulation records. |
| ADR-047 | Accepted | Dynamic schedule arbitration driven by five core resident needs | Tracks Hunger, Energy, Social, Hygiene, and Purpose in pure domain records. Decay and recovery are driven by activities and traits. Critical need depletion overrides rigid schedule blocks so residents autonomously seek diners, rest, or hygiene. |
| ADR-048 | Accepted | Adopt optimistic-but-fragile as the official tone target | The tower can be vibrant and hopeful while weak systems visibly create personal hardship; product language rejects both cartoon villainy and helpless misery. |
| ADR-049 | Accepted | Preserve strictly systems-level player control | The Steward acts through building, zoning, capacity, policy, leasing, and zone-level priorities, never direct individual resident orders. |
| ADR-050 | Accepted | Extend resident wellbeing with readable Satisfaction, Grievances, and personality-filtered Strain | Needs remain the short-term foundation; Satisfaction and Grievances express medium-term conditions; Strain expresses long-term cumulative pressure without requiring an unbounded personality matrix. |
| ADR-051 | Accepted | Use a small high-impact personality facet set | Four to eight facets provide meaningful differential reactions and inspector explanations while remaining legible and aggregation-safe at 300 residents. |
| ADR-052 | Accepted | Introduce Scrutiny as an external-pressure resource | Wider-city attention responds to expansion, inequality, unresolved crises, policy, and service outcomes; it modulates pressure without a cartoon-invasion loop. |
| ADR-053 | Accepted | Make specialist roles emerge from training and capacity systems | Residents acquire useful roles through tower conditions rather than direct assignment, preserving the systems-only control rule. |
| ADR-054 | Accepted | Keep relationships, factions, and crises aggregation-safe and systems-driven | Relationships and factions combine aggregate state with notable-individual sampling; adaptive crises create personal consequences through needs and strain. |
| ADR-055 | Accepted | Standardise the cause-chain inspector as the explanation contract | Every important problem flows from world symptom through overlay and inspector cause to a contextual systems-level response, using immutable explanation-ready projections. |
| ADR-056 | Accepted | Project population demographics from stable IDs and household resource state | Population overlays remain immutable Application projections: age cohorts are deterministic roster metadata derived from stable resident IDs, while resource bands expose the existing household budget state instead of adding mutable presentation-owned demographics. |
| ADR-057 | Accepted | Derive electrical connectivity from utility rooms in topology | A ground-floor substation supplies a same-column continuous riser and per-floor transformer; the pure-C# evaluator produces immutable voltage and brownout-cause projections, keeping infrastructure state saveable through existing topology serialization. |
| ADR-058 | Accepted | Derive water and waste networks from utility rooms in topology | A ground pump supplies a continuous riser with optional booster resets; a continuous gravity chute terminates at a ground collector. The projection is immutable, immediately tracks topology changes, needs no save-schema change, and provides stable causes for OR-803. |
| ADR-059 | Accepted | Expand the ground slab in place instead of stacking new ground floors | Ground expansion must contain the existing slab, charges only new cells, is scrutiny-gated like other expansion, deducts before mutating with refund on failure, and persists through existing slab serialization without a schema change. |
| ADR-060 | Accepted | Persist utility-ops equipment condition through the save envelope | OR-803 shipped wear/repair as session-only state; failures vanished on load. `UtilityOperationsState` now owns `ToSaveData`/`FromSaveData` (ARCH-004 pattern) with a nullable `utilityOperations` field so older saves restore to fresh defaults. |
| ADR-061 | Accepted | Remove composite placeholder resident sprites; Spine modular rig is the sole NPC path | The 6 per-archetype composites plus the combined sheet duplicated the Spine 2D hierarchy (torso/head/limbs + 8 wardrobe slots) while `NpcSkeletalHierarchy.MainRenderer` stayed disabled. Deleted all 7 runtime PNGs, emptied `NpcContentRegistry.ResourcePath` (content IDs immutable), made `ResidentSpriteCatalog` skip empty paths to its procedural fallback, exempted residents from the `AssetLabValidator` Resources-existence gate, and emptied the `residents` Addressables group. |
| ADR-062 | Accepted | Ship modular-sheet diversity as deterministic wardrobe-variant palettes, not sliced sprites | The 5 paperdoll sheets (front/side/back, 1536x1024) are opaque-gradient reference, failing the Docs/05 transparency gate, so direct slicing would bake halos into the 0.58m cutaway cast. `NpcWardrobeVariantCatalog` captures their 12 profession columns as pure-Content hex palettes driving the existing 8-layer compositor (slot colors + skin-tone anatomy + +/-5% stature); the 6 `npc.resident.*.v1` IDs stay immutable and spec `resident-wardrobe-modular-v1.spec.json` stays `proposed-source-art` until artist cleanup lands transparent parts. |
| ADR-063 | Accepted | Slice front paperdoll sheet into 49 transparent wardrobe parts bound to rig slots | Uniform-grid measurement (8-col pitch 140.25, 9-col pitch 124.67) plus per-cell rembg u2net removal, largest-component filtering, and transparent trim produced 49 validated parts (8 headgear, 12 heads, 13 uppers, 9 lowers, 7 footwear pairs) at <=256px, bottom-center pivot, 512 PPU under `Resources/Residents/Wardrobe/Front`. `WardrobePartCatalog` auto-fits parts to per-layer target sizes with length-aware lower anchors; covered procedural anatomy hides; palette swatches stay the fallback. Side/back views remain reference until facing-swap needs them. |
| ADR-064 | Accepted | Permit `unity` pipeline CLI commands as validation evidence | The headless-only rule predated a working setup: `com.unity.pipeline` 0.7.0-exp.1 is installed and `unity test`/`unity command` drive the same editor batch runs without a Pipeline server dependency. Raw one-shot runs remain the default; connected editors belonging to other sessions are read-only territory. |
| ADR-065 | Accepted | Scrutiny is event pressure, never an instant expansion veto | Live play pinned Scrutiny at 100% and the `CanExecute` threshold turned a pressure signal into a permanent global build lock with no in-Build-mode explanation. Removed the automatic constraint (supersedes the gating half of ADR-059); high scrutiny now only raises inspection-event likelihood for the OR-1002 crisis system, surfaced through the overlay band and inspector. |
| ADR-066 | Accepted | Retune Scrutiny dynamics against saturation; surface hover rejection reasons in Build mode | Follow-up to ADR-065: (1) softened the unresolved-pressure curve (`grievances/3 + strain/2`), shrank flat relief, and added `Value`-proportional mean-reversion so calm towers settle low, busy towers moderate, and single expansions bump visibly — an untouched 600-tick tower must stay under 80%. (2) `GridPlacementController` now keeps the `CanExecute` failure reason and draws a cursor-following `✖ reason` / `✔ target` hint (non-colour prefix), closing the silent-red-ghost gap. |
| ADR-067 | Accepted | Close the 24/7 daily-cycle loop for resident behaviour | The modulo schedule already repeated, but residents stranded in transit across a block boundary missed their trip until the next boundary, satisfied eaters sat in the diner indefinitely mid-block, overnight Hygiene decayed with no recovery path, arrivals via Hygiene landed Idle instead of resting, and all Food/Leisure demand pinned a single room. Fix at the existing seams without new state: mid-block meal-complete returns plus stranded Sleeping/Working/Leisure catch-up in `DynamicScheduleArbitrator` (Idle stays silent mid-block, preserving ADR-020 statelessness), Hygiene recovery while Sleeping at home, arrival-activity reconciliation in `TransitExecutionSystem`, and deterministic person-ID spread across matching diners/lobbies in `ScheduleTripGenerator`. |
| ADR-068 | Accepted | Expose the daily cycle as a pure day/night presentation clock | `DayClock.FromTick` maps ticks onto the existing `DailySchedule.TicksPerDay` (1440, tick 0 = midnight) as an immutable `DayPhase` (day number, HH:MM, night 20:00–05:59) with zero simulation state and no behaviour change. `TowerAtmospherePresenter.UpdateDayNight` quantizes to 30-minute buckets, warms window-light cones at night, and fades a collider-free night tint (alpha ≤ 0.34); the HUD shows `Day N — HH:MM Day/Night`. No scene-light dependency (Tower is all-Unlit 2D). |
| ADR-069 | Accepted | Own TowerSimulation inside the session and project topology once per structural change | Direct access to mutable aggregates invalidated congestion and resident caches without changing the tick. The session creates its simulation through owned fixtures, exposes immutable room/slab geometry and copied resident inspection facts to callers, and invalidates transit/congestion projections by version on accepted commands and ticks. Structural projections stay stable across ticks; fixture construction does not change schedules or random streams through a save round-trip. |
| ADR-070 | Accepted | Construct Data-mode explanations in one Application module | Seven independent overlay builders multiplied wiring in the Tower coordinator and spread the symptom-to-cause contract across files. `TowerDataOverlays` owns typed, lazy projections for transit, residents, Scrutiny, businesses, and utilities; the elevator projection is cached by congestion identity so HUD and inspector reads reuse it. |
| ADR-071 | Accepted | Model Outside as a first-class world endpoint | Residents currently begin in assigned rooms and trips require room IDs. Define `Outside` as a persistent Domain location connected to the tower through the ground-floor lobby entrance; move-ins and external commutes use this boundary, with a compact street-edge node as the initial world representation and street view/horizon as Presentation. Never encode Outside as an invalid room ID. |
| ADR-072 | Accepted | Add typed endpoint fields to the existing save DTOs | OR-1004 adds `WorldLocation` for residents and trips, with the street-edge graph node connected only to the lobby portal. Save DTOs add explicit location-kind fields and retain room ID fields for room endpoints; missing kinds from older saves default to Room. Active routes are rebuilt from typed endpoints on load, so graph node IDs are not persisted. Demand-created residents begin Outside, and a missing internal workplace becomes an external job rather than a home-as-work shortcut. |
| ADR-073 | Accepted | Present Outside as layered procedural cutaway scenery | The tower uses unlit quads, so `OutsideCityPresenter` generates three collider-free silhouette layers, irregular window grids, roof details, and lamps against the camera background instead of introducing unmanaged source art. The skyline stays near the lobby's visual scale, avoiding a hard-edged sky rectangle. Horizontal camera pans shift only three roots at depth-specific rates; facade and window colour changes use half-hour buckets. The overview camera shifts right to leave a visible city slice while keeping the tower in frame. No skyline object enters simulation or save data. |
| ADR-074 | Accepted | Steward UI shell and theme standardization | Replaces debug dashboard with compact steward status card, categorizes build palette into four groups (Space & Use, Services, Utilities, Movement & Removal), unifies mode dock (Build, Inspect, Data, Manage) under StewardTheme, and enables 7 selectable overlays in Data mode without direct simulation mutation. |

